/**
 * Whether the reviews section is shown.
 *
 * The quotes in `src/i18n/ui.ts` are placeholders, written to lay the
 * section out while real ones are collected. Invented testimonials on a
 * real contractor's site are worse than no testimonials, so the section
 * stays off the page until this is flipped.
 *
 * To turn it on: replace the `review.*` strings in all four locales with
 * what clients actually said, then set this to true. Nothing else changes.
 */
export const showReviews = false;
